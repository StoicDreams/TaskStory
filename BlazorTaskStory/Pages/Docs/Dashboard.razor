@inherits BUIPage
@page "/docs/dashboard"

<DocDashboard />

@code {
	protected override Task OnInitializedAsync()
	{
		Title = PageTitles.DocDashboard;
		ContinueButton = ("/docs/projects", PageTitles.DocProjects);
		return base.OnInitializedAsync();
	}
}
